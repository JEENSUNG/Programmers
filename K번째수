func solution(array []int, commands [][]int) []int {
    int size = array.length();
    int length = commands.length;
    int[][] arr = new int[size][size];
    for(int i = 0; i < length; i++) {
        for(int j = 0; j < commands[i][0] - commands[i][1])
        }
    }
    return []int{}
}
